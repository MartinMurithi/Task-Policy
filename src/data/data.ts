
export enum policyTypeEnum {
    life = 'life',
    health = 'health',
    auto = 'auto',
    home = "home"
};

export enum policyStatus {
    active = "active",
    expired = "expired",
    cancelled = "cancelled"
}

export enum statusEnum { 
    submitted = "submmited",
    approved = "approved",
    rejected = "rejected",
    paid = "paid"
}