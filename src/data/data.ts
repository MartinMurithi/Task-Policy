
export enum policyTypeEnum {
    life = 'life',
    health = 'health',
    auto = 'auto',
    home = "home"
};

export enum policyStatusEnum {
    active = "active",
    expired = "expired",
    cancelled = "cancelled"
}

export enum claimStatusEnum { 
    submitted = "submmited",
    approved = "approved",
    rejected = "rejected",
    paid = "paid"
}